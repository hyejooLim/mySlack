import React from 'react';

import { ChatListZone, Section, StickyHeader } from './style';

const ChatList = () => {
  return (
    <ChatListZone>
      <StickyHeader>
        <button></button>
      </StickyHeader>
      <Section></Section>
    </ChatListZone>
  );
};

export default ChatList;
